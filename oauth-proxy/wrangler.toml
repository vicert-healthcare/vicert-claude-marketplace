name = "vicert-oauth-proxy"
main = "src/index.ts"
compatibility_date = "2024-09-01"

[vars]
GITHUB_CLIENT_ID = "Ov23li7vmJRWJ58UhThw"
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS = "https://vicert-healthcare.github.io,http://localhost:4321"

# GITHUB_CLIENT_SECRET must be set via: npx wrangler secret put GITHUB_CLIENT_SECRET
